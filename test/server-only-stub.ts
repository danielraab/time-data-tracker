// Stub for the `server-only` package so server-side modules can be imported in
// the Vitest (Node) test environment without throwing.
// This file is referenced by the `server-only` alias in vitest.config.ts.
export {};
